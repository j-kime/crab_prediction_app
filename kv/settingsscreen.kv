<SettingsScreen>:
    # PLACE HOLDER
    #REQURIED ACTIONS: CHANGE UNITS OF EACH METRIC; CHANGE MODEL TYPE (LOGISTIC OR LINEAR REGRESSION);
    Button:
        text: "You are on the settings screen"
        on_release: app.change_screen("home_screen")